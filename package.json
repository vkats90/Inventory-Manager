{
  "name": "inventory-manager",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd Inventory-Manager-Backend && npm install && npm run build"
  },
  "dependencies": {
    "apollo-server-express": "^2.25.2",
    "express": "^4.17.1"
  }
}
